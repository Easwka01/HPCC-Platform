package com.lexisnexis.platform.eclwatch;

//import autoitx4java.AutoItX; 
//import autoit-0.1.13;
import static org.junit.Assert.assertEquals;

import java.util.concurrent.TimeUnit;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.BlockJUnit4ClassRunner;
import org.openqa.selenium.By;

import com.lexisnexis.platform.eclwatch.testharness.Timeout;
import com.lexisnexis.platform.eclwatch.testharness.Timeout.Group;

/*
 * @RunWith(BlockJUnit4ClassRunner.class) public class WorkunitThorSlaveLogsTest extends TestBase {
 * 
 * @Test
 * 
 * @Timeout(group = Group.SLOW) public void testWUIDThorSlaveLogsTest() throws Exception { String expectedTitle =
 * "ECL Watch"; // Login to Ecl Watch driver.navigate().to(appUrl); Thread.sleep(3000);
 * 
 * // Search Workunit driver.findElement(By.id("stubFindText")).sendKeys("W20161219-113845");
 * System.out.println("workunit"); driver.findElement(By.xpath(".//*[@id='stubFind']/span[1]")).click();
 * Thread.sleep(5000); // driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS); //
 * driver.findElement(By.id("dijitReset dijitInline dijitIcon iconFind")).click();
 * System.out.println("Workunit Page Loaded");
 * 
 * // Slave logs pop-up box
 * driver.findElement(By.id("stub_Main-DL_Search-DL_DetailW20161219x113845-DLSlaveLogs_label")).click();
 * Thread.sleep(2000);
 * 
 * // Verify the pop up box assertEquals(expectedTitle, driver.getTitle()); System.out.println("Title Verified");
 * 
 * waitForWUIDSlaveLogsDialogBox(); System.out.println("Table loaded"); Thread.sleep(2000);
 * waitForSlaveLogsTableToLoad(); Thread.sleep(3000); assertEquals("Thor Process:",
 * driver.findElement(By.xpath(".//*[@id='hpcc_TableContainer_96']/table/tbody/tr[1]/td[1]")).getText().trim()); //
 * assertEquals("Slave Number:", //
 * driver.findElement(By.xpath(".//*[@id='hpcc_TableContainer_96']/table/tbody/tr[2]/td[1]/label")).getText().trim());
 * // assertEquals("File:", //
 * driver.findElement(By.xpath(".//*[@id='hpcc_TableContainer_10']/table/tbody/tr[3]/td[1]")).getText().trim());
 * 
 * } }
 */